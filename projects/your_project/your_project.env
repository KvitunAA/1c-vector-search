# Конфигурация профиля your_project
# Переименуйте папку и файл под свой проект (например: Vector/Vector.env)
# Для переопределения путей на другой машине создайте your_project.env.local (см. PORTABILITY.md)

# === ОБЯЗАТЕЛЬНЫЕ ПАРАМЕТРЫ ===
# Путь к выгрузке 1С: основная конфигурация или расширение (в корне — Configuration.xml)
CONFIG_PATH=C:\path\to\your\1c\config

# === MCP (имя сервера в Cursor; для каждой конфигурации — своё имя) ===
# MCP_SERVER_NAME=your_project
# CONFIG_DESCRIPTION=MCP: семантический поиск по вашей конфигурации 1С

# === РАСШИРЕНИЕ (отдельные БД; индексация: run_index_extension_*.cmd) ===
# EXTENSION_CONFIG_PATH=C:\path\to\extension\dump
#
# === ОБЪЕДИНЁННАЯ ИНДЕКСАЦИЯ (один MCP, одна БД: конфа + все расширения) ===
# run_index_unified_your_project.cmd
# EXTENSIONS_ROOT=C:\path\to\folder_with_extension_subdirs
# или EXTENSION_CONFIG_PATHS=C:\path\to\ext1;C:\path\to\ext2
#
# EXTENSION_VECTORDB_PATH по умолчанию: projects/your_project/extension_vectordb
# EXTENSION_GRAPHDB_PATH по умолчанию: projects/your_project/extension_graphdb/graph.db

# === ЭМБЕДДИНГИ ===
# Вариант 1: Удалённый API (LM Studio, LocalAI, OpenAI-совместимый)
# EMBEDDING_API_BASE=http://your-host:port/v1
# EMBEDDING_MODEL=your-embedding-model-name
# EMBEDDING_DIMENSION определяется автоматически по имени модели.
# Явно задавайте, только если модель не распознана (см. KNOWN_MODELS в config.py).
# EMBEDDING_DIMENSION=768

# Вариант 2: Локальная модель (sentence-transformers)
# EMBEDDING_API_BASE=
# EMBEDDING_MODEL=your-embedding-model-name

# Qwen3 через LM Studio / GGUF:
# llama.cpp уже добавляет EOS автоматически на уровне токенизатора.
# Если true — получится двойной EOS ("Added a EOS token... 2 EOS tokens").
# Предупреждение "add_eos_token should be true" — косметическое, на качество не влияет.
# Оставьте false (или не задавайте). Подробнее: MODEL_CONFIGURATION_RECOMMENDATIONS.md, раздел 5.
# EMBEDDING_ADD_EOS_MANUAL=false

# === ОПЦИОНАЛЬНЫЕ ===
# VECTORDB_PATH по умолчанию: projects/your_project/vectordb (sqlite-vec, файл vectordb.sqlite внутри)
# GRAPHDB_PATH по умолчанию: projects/your_project/graphdb/graph.db (каталог БД Kuzu)
# GRAPH_STAGING_PATH по умолчанию: projects/your_project/graph_staging (для index_graph_mp.py --staging)
#
# === ГРАФ (индексация index_graph_mp.py) ===
# GRAPH_WRITE_BATCH_SIZE=5000
# GRAPH_CSV_BATCH_SIZE=50
# GRAPH_LARGE_GRAPH_THRESHOLD_MB=300
# GRAPH_FLUSH_CLOSE_EVERY=100
# GRAPH_MODULE_CHUNK_SIZE=100
# GRAPH_STREAM_BATCH_SIZE=200
# GRAPH_RELEASE_EVERY_N_BATCHES=4
# KUZU_BUFFER_POOL_SIZE=0
# Компактная сборка без MERGE-bloat: set STAGING=1 в run_index_graph_*.cmd → --staging
#
# Чанки и контекст (см. MODEL_CONFIGURATION_RECOMMENDATIONS.md):
# - nomic-embed-text-v2-moe / MiniLM (512 токенов): EMBEDDING_MAX_TOKENS=512, CHUNK_MAX_TOKENS=512
# - nomic-embed-text-v1 (8192 токенов): EMBEDDING_MAX_TOKENS=1024, CHUNK_MAX_TOKENS=1024
# CHARS_PER_TOKEN — эвристика симв/токен (по умолчанию 1.65; для кириллицы/BSL не полагайтесь на 2.0)
# EMBEDDING_MAX_TOKENS=512
# CHUNK_MAX_TOKENS=512
# CHUNK_OVERLAP_TOKENS=100
# CHARS_PER_TOKEN=1.65
# Альтернатива: задать EMBEDDING_MAX_CHARS напрямую (например 850–900 при лимите 512 токенов у модели)
EMBEDDING_MAX_CHARS=1024
# Размер батчей индексации (влияет на скорость и пиковое потребление RAM):
# 8 GB: 25/20/20 | 16 GB: 50/50/50 | 32 GB: 100/100/100 | 48+ GB: 200/150/150
# BATCH_SIZE_CODE=100
# BATCH_SIZE_METADATA=50
# BATCH_SIZE_FORMS=50
DEFAULT_SEARCH_LIMIT=5
MAX_SEARCH_LIMIT=20

# Поиск: метрика расстояния (cosine|l2|ip), гибрид вектор+BM25, MMR
# По умолчанию: гибрид вектор+BM25 (alpha=0.7) + MMR (lambda=0.7) — включены автоматически.
# Настройки ниже переопределяют дефолты при необходимости.
# VECTOR_DISTANCE_METRIC=cosine
# HYBRID_SEARCH_ALPHA=0.7
# SEARCH_USE_MMR=true
# MMR_LAMBDA=0.7
# SEARCH_FETCH_K=50

LOG_LEVEL=INFO
